<div>
  <select class="form-select" id="tag">
    <option selected>Product Tags</option>
    {{#each products}}
    <option value="{{tagId}}">{{tag.tag_name}}</option>
    {{/each}}
  </select>
</div>
